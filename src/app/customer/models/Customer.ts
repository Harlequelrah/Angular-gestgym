export class Customer {
    constructor(public id: number, public first_name: string, public last_name: string, public registration_date: string, public phone_number: string, public active_suscription: boolean) { }
}
